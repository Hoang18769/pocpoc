/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['pngdownload.io','https://picsum.photos/'],  // Thêm domain bạn muốn load ảnh từ đó
  },
};

export default nextConfig;